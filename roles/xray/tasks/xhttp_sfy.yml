---
- include_tasks: vless_tls_identity.yml

- name: Generate config
  ansible.builtin.template:
    src: xhttp_sfy.json.j2
    dest: "{{ xray_config_path }}"
  notify:
    - Enable and start Xray
    - Restart xray
