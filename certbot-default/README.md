# certbot-default

Assumed that `certbot` installed via `install-cerbot` playbook.

<https://certbot.eff.org/instructions?ws=nginx&os=pip&tab=standard>

- Gets `certbot` certificate.
- `nginx`, `pip`, autorenewal, autoupdate.
