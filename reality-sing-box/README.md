# reality-sing-box

Like `reality-sfy-sing-box`, but without `steal from yourself`. Masquerading as another domain.
Can be errors on client, because many TLS handshakes with the another domain, depends on domain.
Anyway, you can use `tls-sing-box`.
